language: cpp

sudo: required
dist: trusty

compiler:
  - clang
  - gcc

before_install:
  - ./scripts/travis-before-install.sh

script:
  - autoreconf --install
  - CPPFLAGS='-Werror' ./configure --enable-sound --enable-warnings
  - make
  - ./run-tests
  - make distcheck
